{
  "emoji": "⚖️",
  "requires": {
    "pip": ["simmer-sdk", "requests"],
    "env": ["SIMMER_API_KEY"]
  },
  "cron": "*/5 * * * *",
  "automaton": {
    "entrypoint": "kalshi_divergence.py",
    "args": ["--live", "--quiet"]
  }
}
